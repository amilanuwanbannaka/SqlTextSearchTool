# SqlTextSearchTool
A lightweight C# console utility that searches all varchar and nvarchar columns in a SQL Server database for a given text and logs matching table-column names to a text file. Ideal for developers debugging data issues or tracing where specific text values exist in a large database.
